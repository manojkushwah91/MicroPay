apiVersion: apps/v1
kind: Deployment
metadata:
  name: transaction-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transaction-service
  template:
    metadata:
      labels:
        app: transaction-service
    spec:
      containers:
        - name: transaction-service
          image: nexus.example.com/micropay/transaction-service:latest
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: transaction-service-config
          env:
            # Config Server connection
            - name: SPRING_CONFIG_IMPORT
              value: "optional:configserver:http://config-server:8888"
            
            # Database URL (matches ${TRANSACTION_DB_URL} in config-repo/transaction-service.yml)
            - name: TRANSACTION_DB_URL
              value: "jdbc:postgresql://postgres:5432/micropay_db"
            
            # Database credentials (matches ${POSTGRES_USER} and ${POSTGRES_PASSWORD} in config-repo)
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: micropay-secrets
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: micropay-secrets
                  key: POSTGRES_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: transaction-service
spec:
  selector:
    app: transaction-service
  ports:
    - port: 8084
      targetPort: 8084
  type: ClusterIP
